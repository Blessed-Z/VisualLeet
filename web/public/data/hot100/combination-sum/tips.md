# 💡 组合总和巧记口诀

### 🌟 核心技巧：
> **“排序加剪枝，速度飞一般；选了还能选，递归不加一；总和若超标，后面全省去。”**

### 🧠 记忆关键点：
1.  **无限使用**：在递归时传递 `i` 而不是 `i+1`，表示当前数字下次还能接着用。
2.  **剪枝优化**：先对 `candidates` 排序。如果 `total + candidates[i] > target`，直接 `break` 掉循环，因为后面的数更大，肯定也超标。
3.  **防止重复**：虽然数字能无限用，但通过 `start_index` 保证搜索顺序，避免搜出顺序不同的重复组合。
