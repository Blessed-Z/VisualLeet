# 💡 最长子串 - 思路巧记

## 🔑 核心口诀
> **右指针跑不停，遇到重复左缩进。**
> **哈希表里记位置，刷新纪录最长心。**

## 🌟 关键点
1. **滑动窗口**：通过 `i` 和 `j` 维护一个有效的动态区间。
2. **哈希表加速**：直接记录位置，让左边界 `i` 能够实现“闪现”跳转，而不是一格格挪。
3. **i 的单向性**：注意 `i = max(i, dic[s[j]])`，左边界永远不会往回退。
