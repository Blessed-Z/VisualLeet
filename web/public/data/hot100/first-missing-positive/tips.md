# 💡 缺失的正数 - 思路巧记

## 🔑 核心口诀
> **数组哈希原地做，数字回座莫啰嗦。**
> **数字 X 归 X-1，不符范围由它过。**
> **坐好之后查一遍，谁家空缺谁闯祸。**

## 🌟 关键点
1. **索引偏移**：数字从 1 开始，索引从 0 开始，所以是 `nums[i]` 对应 `i+1`。
2. **死循环预防**：交换前一定要检查 `nums[i] != nums[nums[i]-1]`，否则两个相同的数字会互相踢皮球。
3. **Hard 题不难**：掌握了置换法，这道 Hard 题其实就是两次遍历。
