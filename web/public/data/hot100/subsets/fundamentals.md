# 📚 子集基础百科

### 🌟 核心概念：幂集 (Power Set)
一个集合的所有子集构成的集合，就叫幂集。比如 `{1, 2}` 的幂集是 `[[], [1], [2], [1, 2]]`。

---

### 📖 变量小字典：

*   **`index`**: 游标。告诉机器人，这次只能从数组的第几个位置开始往后选，绝对不能往回选（否则会搜出重复组合）。
*   **`path`**: 当前的篮子快照。
*   **`nums`**: 原始的水果摊。

---

### 🔍 递归深度：
子集问题的递归树，每一层都代表我们“选了一个数”。树的高度最大就是 `nums` 的长度。
