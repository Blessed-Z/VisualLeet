# 💡 最小覆盖子串 - 思路巧记

## 🔑 核心口诀
> **右边狂奔找全家，左边精简省地儿花。**
> **Counter 比较真神力，覆盖瞬间记下它。**

## 🌟 关键点
1. **Counter 比较**：`cnt_s >= cnt_t` 是 Python 的黑魔法，能直接判断是否覆盖。
2. **两阶段滑动**：第一阶段（寻找）由 `right` 负责，第二阶段（优化）由 `left` 负责。
3. **初始化边界**：`ans_right` 设为 `len(s)`，方便第一次比大小时更新。
