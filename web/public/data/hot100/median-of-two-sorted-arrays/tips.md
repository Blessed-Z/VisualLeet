# 💡 寻找中位数巧记口诀

### 🌟 核心技巧：
> **“短数组上做二分，切开两半定乾坤。左大对右小，交叉验证最精分；奇数取最大，偶数均值真。”**

### 🧠 记忆关键点：
1. **短数组优先**：必须确保 `n1 <= n2`，这样在寻找 `m2 = k - m1` 时，`m2` 才不会变成负数。
2. **切分点含义**：`m1` 表示第一个数组左半部分有几个元素。
3. **边界极端值**：当切分点在开头或结尾时，用 `-inf` 或 `inf` 来作为哨兵，方便比较逻辑。
