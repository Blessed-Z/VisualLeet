# 💡 最长连续序列 - 思路巧记

## 🔑 核心口诀
> **数字乱序莫发愁，哈希集合口袋收。**
> **寻觅起点看前数，顺藤摸瓜数排头。**

## 🌟 关键点
1. **去重与加速**：使用 `set(nums)` 去除重复数字，并提供 $O(1)$ 的查找速度。
2. **只找起点**：`if num - 1 not in num_set` 是本题的灵魂，避免了多余的重复计算。
3. **动态更新**：使用 `max()` 不断刷新最长序列纪录。
