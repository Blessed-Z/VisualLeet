# 💡 找到异位词 - 思路巧记

## 🔑 核心口诀
> **Counter 在手不用愁，目标成分记心头。**
> **右进一，左出一，配方一致答案收。**

## 🌟 关键点
1. **Pythonic 工具**：`collections.Counter` 是处理频率统计的神器。
2. **优雅的循环**：`for right, c in enumerate(s)` 一次性搞定索引和字母。
3. **动态维护**：不需要每次重新数，只需要在滑动时对 `left` 和 `right` 位置的字符做增减。
