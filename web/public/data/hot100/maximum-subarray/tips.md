# 💡 最大子数组和 - 思路巧记

## 🔑 核心口诀
> **前缀若是负能量，果断舍弃奔前方。**
> **若是正能齐上阵，累加记录最高强。**

## 🌟 关键点
1. **状态定义**：`dp[i]` 必须是以 `nums[i]` 结尾的子数组和。
2. **状态转移**：`dp[i] = max(nums[i], dp[i-1] + nums[i])`。
3. **空间优化**：发现 `dp[i]` 只和 `dp[i-1]` 有关，其实可以用一个变量代替数组。
