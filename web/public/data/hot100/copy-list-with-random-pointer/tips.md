# 💡 复制链表巧记口诀

### 🌟 核心技巧：
> **“一过做替身，二过连丝线；字典做导航，克隆在手间。”**

### 🧠 记忆关键点：
1.  **分步走：** 不要急着一遍就把线全连好，第一遍只造人，第二遍再连线。
2.  **字典法：** 字典里的 `key` 是旧节点，`value` 是新节点。
3.  **安全感：** 用 `dic.get(node)` 即使遇到 `None` 也不怕，会自动返回 `None`。
