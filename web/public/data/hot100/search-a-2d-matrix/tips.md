# 💡 搜索矩阵巧记口诀

### 🌟 核心技巧：
> **“右上起点看分明，大小判断路自清；小了下移换一排，大了左移避险情；楼梯步步稳准狠，宝藏终究现原形。”**

### 🧠 记忆关键点：
1. **起点选择**：右上角 `(0, n-1)` 是关键。因为在这里，往左是变小，往下是变大，具有明确的指向性。
2. **逻辑对比**：当前值 `< target` 时，这一行都没戏了，所以 `i++`；当前值 `> target` 时，这一列都没戏了，所以 `j--`。
3. **范围限制**：注意 `while` 循环的边界 `i < m and j >= 0`。
