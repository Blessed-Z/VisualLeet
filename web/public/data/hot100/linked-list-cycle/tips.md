# 💡 环形链表 - 思路巧记

## 🔑 核心口诀
> **龟兔赛跑在环途，兔子双倍步步趋。**
> **若是直线终有尽，若是圆圈必相遇。**

## 🌟 关键点
1. **速度差**：`fast` 必须比 `slow` 快，且通常设为 2 倍速。
2. **安全检查**：每次移动 `fast` 前都要检查 `fast.next`，防止“跳进虚空”报错。
3. **判等时机**：代码里可以先移动再判等，也可以初始时让 `fast = head.next`。
