# 💡 反转链表 - 思路巧记

## 🔑 核心口诀
> **递归到底找新头，邻居反手把你留。**
> **莫忘指向断后路，层层返回功名就。**

## 🌟 关键点
1. **递归基**：`if not head or not head.next`。
2. **反向指引**：`head.next.next = head`。
3. **安全断开**：`head.next = None`（如果不写这一行，会产生死循环环路！）。
