# 💡 单词搜索巧记口诀

### 🌟 核心技巧：
> **“网格搜索 DFS，起点不固定；字符不符快撤退，越界检查别大意；进门先变空字符，出门还原老样子。”**

### 🧠 记忆关键点：
1.  **内外循环**：外层两个 `for` 循环遍历所有格子，内层 `dfs` 处理递归。
2.  **原地回溯**：使用 `board[i][j] = ''` 暂时代替 `visited` 数组，但离开时必须改回 `word[k]`。
3.  **终止条件**：`board[i][j] != word[k]` 立即失败；`k == len(word) - 1` 且字符匹配则成功。
