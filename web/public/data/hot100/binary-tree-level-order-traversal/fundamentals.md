# 基础百科
- **广度优先搜索 (BFS)**：从根节点开始，沿着树的宽度遍历树的节点。如果所有边的权重相等，BFS 可以找到最短路径。
- **队列 (Queue)**：一种“先进先出”（FIFO）的数据结构，就像排队买票。
- **变量字典**：
  - `queue`: 存放待访问节点的队列。
  - `level_size`: 当前这一层有多少个节点。
  - `level`: 存放当前层节点值的列表。
  - `res`: 最终的大清单，包含每一层的清单。
