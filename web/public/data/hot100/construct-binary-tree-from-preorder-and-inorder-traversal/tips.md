# 💡 构造树巧记口诀

### 🌟 核心技巧：
> **“前序找根，中序分边；左队对左队，右队对右队。”**

### 🧠 记忆关键点：
1.  **第一个就是根**：`preorder[0]` 永远是当前子树的根。
2.  **切西瓜**：在中序数组里找到根的位置 `k`，左边 `k` 个就是左子树，右边剩下的是右子树。
3.  **对齐索引**：前序的 `[1:k+1]` 对应中序的 `[0:k]`，这是最容易写错的地方！
