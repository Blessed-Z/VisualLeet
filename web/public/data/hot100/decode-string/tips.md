# 💡 字符串解码巧记口诀

### 🌟 核心技巧：
> **“数字存倍数，左括号存进度；字母接后面，右括号大变身；弹出旧零件，重复新花样。”**

### 🧠 记忆关键点：
1.  **多位数字处理**：`num = num * 10 + int(ch)`，处理像 `100[a]` 这样的数字。
2.  **入栈时机**：遇到 `[` 时，把 `res` 和 `num` 同时入栈，并清零。
3.  **出栈合并**：遇到 `]` 时，`res = last_res + cur_num * res`。
