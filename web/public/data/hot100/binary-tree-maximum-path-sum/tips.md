# 💡 最大路径和巧记口诀

### 🌟 核心技巧：
> **“左贡献，右贡献；负数通通变零蛋；左右根加满，记录创新高；回传给爸爸，只能挑一边。”**

### 🧠 记忆关键点：
1.  **贡献值 vs 路径和**：这是两个概念。回传给爸爸的是“贡献值”（单链），用来更新全局记录的是“桥总和”（双链）。
2.  **负数处理**：`max(0, val)` 是灵魂，代表如果不划算就不选这个分支。
3.  **全局更新**：定义一个全局或外部变量 `ans`，在递归过程中不断更新它。
