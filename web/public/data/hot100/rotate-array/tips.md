# 💡 轮转数组 - 思路巧记

## 🔑 核心口诀
> **步数太大取个余，切片魔法变瞬移。**
> **尾巴搬到最前方，原位修改真神气。**

## 🌟 关键点
1. **k = k % n**：这是必须要做的第一步，防止步数超出数组长度导致报错。
2. **切片赋值**：`nums[:0] = ...` 是一种在列表开头插入多个元素的骚操作。
3. **del 语句**：用于删除列表中的一段范围，实现“原位”修改。
