# 💡 旋转图像 - 思路巧记

## 🔑 核心口诀
> **旋转图像九十度，原地挪动莫迷路。**
> **左上四分定乾坤，四角轮换有神助。**
> **坐标变换记心中，一行赋值全托付。**

## 🌟 关键点
1. **原地旋转**：面试官通常禁止使用额外数组，必须直接修改原矩阵。
2. **四向赋值**：`a, b, c, d = d, a, b, c` 这种 Python 语法能极大地简化代码，防止变量覆盖出错。
3. **边界控制**：循环的终点是 `n // 2` 和 `(n + 1) // 2`，这是为了覆盖所有的“旋转组”。
