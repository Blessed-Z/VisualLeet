# 💡 路径总和巧记口诀

### 🌟 核心技巧：
> **“前缀和里藏路径，DFS里带回溯；查查差值有多少，宝藏数量涨不停。”**

### 🧠 记忆关键点：
1.  **初值很重要**：`cnt[0] = 1` 别忘了！这是为了抓到那些从根节点开始就达标的路径。
2.  **看差值**：我们在 `cnt` 里找的是 `s - targetSum`。
3.  **记得撤销**：回来时 `cnt[s] -= 1`，保证不影响其他分支。
