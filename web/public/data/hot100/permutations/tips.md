# 💡 全排列巧记口诀

### 🌟 核心技巧：
> **“回溯大法好，选完要撤销；点名册里查，没到你就跑。”**

### 🧠 记忆关键点：
1.  **全局状态**：用 `path` 记录当前排列，用 `nums[i] not in path` 判断是否可选（这是 O(N) 的，大数据量建议用 boolean 数组 `used`）。
2.  **拷贝副本**：`res.append(path[:])` 这一步极其关键！必须存一个快照，否则最后 `path` 变空，结果里的组合也全变空了。
3.  **三部曲**：做选择 -> 递归 -> 撤销选择。
