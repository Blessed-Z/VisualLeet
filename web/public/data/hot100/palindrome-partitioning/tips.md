# 💡 分割回文串巧记口诀

### 🌟 核心技巧：
> **“回溯大法切香肠，回文判断是关卡；切下一段看正反，一模一样再出发；走到尽头留全影，撤回最后再换家。”**

### 🧠 记忆关键点：
1.  **切片语法**：Python 的 `s[i:j+1]` 配合 `[::-1]` 是判断回文最快的方法。
2.  **递归参数**：`dfs(j+1)` 代表从刚才切断的位置之后继续开始。
3.  **浅拷贝**：`ans.append(path.copy())` 别忘了！必须存快照，否则最后 `path` 变空，结果也全空了。
