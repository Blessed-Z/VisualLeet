# 💡 合并有序链表 - 思路巧记

## 🔑 核心口诀
> **虚拟头结点在前，两路对比找更偏。**
> **谁的数值更娇小，谁就接在尾中间。**
> **一路空了别慌张，另一整队全收编。**

## 🌟 关键点
1. **虚拟头结点**：解决空链表判断的烦恼，代码更清爽。
2. **一次遍历**：利用原有的 `next` 指针重新连接，不需要创建新节点。
3. **逻辑完备**：最后的 `if...else` 挂载剩余链表是提速的关键。
