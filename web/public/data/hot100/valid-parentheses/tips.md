# 💡 有效括号巧记口诀

### 🌟 核心技巧：
> **“左括号进栈，右括号查岗；对得上就消，对不上就凉；最后看兜里，空空才算强。”**

### 🧠 记忆关键点：
1.  **奇数长度必错**：如果字符串长度是单数，肯定配不成对，直接返回 `False`。
2.  **字典做映射**：用 `{')':'(', ']':'[', '}':'{'}` 建立右对左的映射，查起来飞快。
3.  **栈的操作**：`append()` 进栈，`pop()` 出栈。
