# 💡 相交链表 - 思路巧记

## 🔑 核心口诀
> **你走我的路，我走你的路。**
> **终点皆相同，相遇在归途。**

## 🌟 关键点
1. **长度抵消**：通过交换起点，将 `L1 + common + L2` 和 `L2 + common + L1` 变得等长。
2. **逻辑简洁**：代码里不需要计算链表长度差，直接 `pa = pa.next if pa else headB` 即可。
3. **不相交处理**：如果不相交，PA 和 PB 最终都会同时变成 `None`，循环也会结束。
