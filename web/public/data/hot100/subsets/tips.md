# 💡 子集巧记口诀

### 🌟 核心技巧：
> **“子集不求多，一个一个拿；记下当前样，再往后搜索；回溯别忘了，还原好状态。”**

### 🧠 记忆关键点：
1.  **位置不回头**：`index` 记录当前从哪开始选，防止选出重复组合（如 `[1,2]` 和 `[2,1]` 在子集里算一种）。
2.  **步步皆结果**：进入函数的第一件事就是把 `path[:]` 存入结果，因为空集和过程中的中间态都是子集。
3.  **递归边界**：当 `index` 达到数组长度，自然就结束了。
