# 💡 电话组合巧记口诀

### 🌟 核心技巧：
> **“数字对字母，映射要记牢；一层选一个，暗号变清晰；走到最后位，组合入大包。”**

### 🧠 记忆关键点：
1.  **映射表**：事先把 `2-9` 对应的字母存好（可以用列表或字典）。
2.  **递归层级**：第 `i` 层递归处理 `digits[i]`。
3.  **字符连接**：`''.join(path)` 将列表里的字符拼成字符串。
