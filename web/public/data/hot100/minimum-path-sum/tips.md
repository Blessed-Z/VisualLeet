# 💡 最小路径和巧记

### 🎵 押韵口诀
> 寻宝迷宫看步数，最小总和记心中。
> 出发点上抄原数，横排竖排累加行。
> 中间格子挑个小，左边上边比一比。
> 选完小的加自己，路尽功成得最小值。

---

### 🔑 核心关键点
1.  **路径限制**：只能向右或向下走。
2.  **状态定义**：`dp[i][j]` 表示到达坐标 (i, j) 的最小路径总和。
3.  **状态转移**：`dp[i][j] = min(dp[i-1][j], dp[i][j-1]) + grid[i][j]`。
4.  **初始化**：第一行和第一列只能由一个方向到达，所以路径是唯一的累加。
